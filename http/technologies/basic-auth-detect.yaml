id: basic-auth-detect

info:
  name: Basic Auth Detection
  author: w4cky_
  severity: low
  tags: tech,basic-auth,securestack-fast

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 401

      - type: word
        words:
          - "Www-Authenticate:"
        part: header
